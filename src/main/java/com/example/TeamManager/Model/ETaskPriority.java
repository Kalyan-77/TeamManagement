package com.example.TeamManager.Model;

public enum ETaskPriority {
    LOW, //Low priority task
    MEDIUM, //medium priority task(Default)
    HIGH, //High Priority task
    URGENT //urgent priority task that needs immediate attention
}
